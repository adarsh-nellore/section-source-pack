# Design-system manifest (generated)

> Generated by `scripts/generate-manifest.mjs` — do not edit by hand.
> Source: section-source-pack@0.1.0.
> Re-runs on `npm run manifest` and `npm run build` (via prebuild).

This file is the canonical input to `/build-hifi` Beat 1. The skill Reads this single file instead of re-deriving the same information from `src/components/**` + `docs/*.md` on every prototype run.

## Contract docs (headline summaries)

### COMPOSITION.md
> The mechanical contract for how templates compose primitives, so visual consistency holds across pages **without** relying on the page author to remember the rules. This document gives `docs/SPACING.md` teeth. SPACING.md is the *what* (named buckets, alignment principles); this is the *how* (the only legal way to consume them). The `npm run audit` script enforces this contract. If a rule below is 

### NAVIGATION.md
> How routing and link/button wiring works in this system. Three rules, each enforceable. For the underlying composition contract, see [`COMPOSITION.md`](./COMPOSITION.md). For the shells that frame navigation, see [`LAYOUT.md`](./LAYOUT.md). ---

### LAYOUT.md
> Five named structural shells cover ~95% of page-level layouts in this system. Pick the shell that matches the page's information shape, compose the contents using the typography + spacing + pattern primitives, ship. This document explains **what to use when**. For the rules that make this possible (closed-enum primitives, mechanical audit), see [`COMPOSITION.md`](./COMPOSITION.md). For the spacing

### COMPONENTS.md
> Every primitive in the system, organized by family. Use this as the single source of truth for "what's available" before authoring new JSX. The Paper file `PeerAIDesign_DesignSystem+Components` carries the canonical visual for each; the node ID in each row links to the Paper sheet. For the contract on how to USE these components, see [`COMPOSITION.md`](./COMPOSITION.md). For shell-level layouts, s

### SPACING.md
> The underlying 4px scale + named buckets + alignment rules. This document is the *vocabulary*. See [`COMPOSITION.md`](./COMPOSITION.md) for the *grammar* — the closed-enum components (`<Stack gap="bucket">`, `<Cluster gap="bucket">`) that mechanically enforce these buckets in templates, plus the `npm run audit` gate that fails the build when raw `gap-N` / `text-[Npx]` / raw `<h1>` slip through. If

## Available primitives

### @/components/typography

- **Body** — Body.tsx — `BodyProps { children: ReactNode; size?: BodySize; tone?: BodyTone; measured?: boolean; className?: string }`
- **Heading** — Heading.tsx — `HeadingProps { children: ReactNode; size?: HeadingSize; tone?: HeadingTone; className?: string; id?: string }`

### @/components/layout

- **AppShell** — AppShell.tsx — `AppShellProps { topBar?: ReactNode; leftNav?: ReactNode; rightRail?: ReactNode; children?: ReactNode; rightRailWidth?: number; leftNavWidth?: number; className?: string }`
- **Cluster** — Cluster.tsx — `ClusterProps { children: ReactNode; gap?: ClusterGap; align?: ClusterAlign; justify?: ClusterJustify; wrap?: boolean; as?: ElementType; className?: string }`
- **DocFrame** — DocFrame.tsx — `DocFrameProps { tabBar?: ReactNode; children?: ReactNode; bordered?: boolean; padding?: "none" | "sm" | "md" | "lg"; className?: string }`
- **Drawer** — Drawer.tsx — `DrawerProps { open: boolean; onClose: () => void; side?: DrawerSide; size?: DrawerSize; header?: ReactNode; footer?: ReactNode; children?: ReactNode; "aria-label"?: string; className?: string }`
- **LeftNav** — LeftNav.tsx — `LeftNavProps { brand?: ReactNode; sections: NavSection[]; footer?: ReactNode; collapsed?: boolean; className?: string }`
- **LinkButton** — LinkButton.tsx — `(no Props type)`
- **PageContainer** — PageContainer.tsx — `PageContainerProps { size?: PageContainerSize; children?: ReactNode; className?: string }`
- **Popover** — Popover.tsx — `PopoverProps { trigger: ReactNode; children: ReactNode; placement?: PopoverPlacement; triggerType?: PopoverTrigger; showArrow?: boolean; "aria-label"?: string; className?: string }`
- **RightRail** — RightRail.tsx — `RightRailProps { header?: ReactNode; footer?: ReactNode; children?: ReactNode; className?: string }`
- **SplitFrame** — SplitFrame.tsx — `SplitFrameProps { left: ReactNode; right: ReactNode; ratio?: number; bordered?: boolean; className?: string }`
- **Stack** — Stack.tsx — `StackProps { children: ReactNode; gap?: StackGap; align?: StackAlign; justify?: StackJustify; as?: ElementType; className?: string }`
- **TopNav** — TopNav.tsx — `TopNavProps { brand?: ReactNode; breadcrumb?: ReactNode; trailing?: ReactNode; className?: string }`
- **Breadcrumb** — TopNav.tsx — `(no Props type)`

### @/components/ui

- **Accordion** — Accordion.tsx — `AccordionProps { items: AccordionItem[]; mode?: "single" | "multi"; defaultOpen?: string[]; className?: string }`
- **Alert** — Alert.tsx — `AlertProps { tone?: AlertTone; title?: ReactNode; body?: ReactNode; children?: ReactNode; action?: ReactNode; onDismiss?: () => void; compact?: boolean; className?: string }`
- **Avatar** — Avatar.tsx — `AvatarProps { initials?: string; src?: string; alt?: string; size?: AvatarSize; variant?: AvatarVariant; ring?: boolean; children?: ReactNode; className?: string }`
- **Badge** — Badge.tsx — `BadgeProps { children: ReactNode; tone?: BadgeTone; size?: BadgeSize; uppercase?: boolean; className?: string }`
- **Banner** — Banner.tsx — `BannerProps { tone: BannerTone; title: ReactNode; body?: ReactNode; onDismiss?: () => void; action?: ReactNode; className?: string }`
- **Button** — Button.tsx — `(no Props type)`
- **Caption** — Caption.tsx — `CaptionProps { children: ReactNode; size?: CaptionSize; tone?: CaptionTone; className?: string }`
- **Card** — Card.tsx — `(no Props type)`
- **Checkbox** — Checkbox.tsx — `(no Props type)`
- **Dot** — Dot.tsx — `DotProps { color?: DotColor; size?: DotSize; pulse?: boolean; className?: string }`
- **DropdownMenu** — DropdownMenu.tsx — `DropdownMenuProps { trigger: ReactNode; sections: DropdownMenuSection[]; open?: boolean; onOpenChange?: (open: boolean) => void; align?: "start" | "end"; className?: string }`
- **EmptyState** — EmptyState.tsx — `EmptyStateProps { icon?: ReactNode; title: ReactNode; body?: ReactNode; action?: ReactNode; variant?: EmptyStateVariant; iconTint?: "neutral" | "coral"; className?: string }`
- **FormField** — FormField.tsx — `FormFieldProps { label?: ReactNode; children: ReactNode; hint?: ReactNode; error?: ReactNode; required?: boolean; htmlFor?: string; className?: string }`
- **Glyph** — Glyph.tsx — `(no Props type)`
- **Hairline** — Hairline.tsx — `HairlineProps { orientation?: HairlineOrientation; style?: HairlineStyle; label?: ReactNode; className?: string }`
- **IconButton** — IconButton.tsx — `(no Props type)`
- **Input** — Input.tsx — `(no Props type)`
- **KeyChip** — KeyChip.tsx — `KeyChipProps { children?: ReactNode; keys?: ReactNode[]; size?: KeyChipSize; className?: string }`
- **MetaLabel** — MetaLabel.tsx — `MetaLabelProps { children: ReactNode; tone?: MetaLabelTone; className?: string }`
- **MetaText** — MetaText.tsx — `MetaTextProps { children: ReactNode; tone?: MetaTextTone; size?: MetaTextSize; className?: string }`
- **Modal** — Modal.tsx — `ModalProps { open: boolean; title?: ReactNode; description?: ReactNode; size?: ModalSize; onClose?: () => void; footer?: ReactNode; children?: ReactNode; className?: string }`
- **Pagination** — Pagination.tsx — `PaginationProps { current: number; total: number; onPageChange?: (page: number) => void; compact?: boolean; className?: string }`
- **PeerBrand** — PeerBrand.tsx — `PeerBrandProps { size?: PeerBrandSize; markOnly?: boolean; className?: string }`
- **Pill** — Pill.tsx — `PillProps { children?: ReactNode; variant?: PillVariant; size?: PillSize; leadingIcon?: ReactNode; trailingIcon?: ReactNode; onClick?: MouseEventHandler<HTMLButtonElement>; onAuxClick?: MouseEventHandler<HTMLButtonElement>; disabled?: boolean; className?: string; asStatic?: boolean }`
- **ProgressBar** — ProgressBar.tsx — `ProgressBarProps { value: number; tone?: ProgressBarTone; showValue?: boolean; label?: ReactNode; size?: "thin" | "default"; className?: string }`
- **ProgressDots** — ProgressDots.tsx — `ProgressDotsProps { total: number; current: number; connector?: boolean; className?: string }`
- **Radio** — Radio.tsx — `(no Props type)`
- **Row** — Row.tsx — `(no Props type)`
- **SearchInput** — SearchInput.tsx — `(no Props type)`
- **Select** — Select.tsx — `SelectProps { options: SelectOption[]; value?: string; onChange?: (value: string) => void; placeholder?: string; disabled?: boolean; invalid?: boolean; leading?: ReactNode; name?: string; id?: string; className?: string; containerClassName?: string }`
- **Skeleton** — Skeleton.tsx — `SkeletonProps { variant?: SkeletonVariant; width?: number | string; height?: number | string; lines?: number; className?: string }`
- **Slider** — Slider.tsx — `SliderProps { value: number; onChange?: (value: number) => void; min?: number; max?: number; step?: number; showValue?: boolean; label?: ReactNode; className?: string; disabled?: boolean; "aria-label"?: string }`
- **Spinner** — Spinner.tsx — `SpinnerProps { size?: SpinnerSize; tone?: SpinnerTone; label?: string; className?: string }`
- **Stat** — Stat.tsx — `StatProps { label: ReactNode; value: ReactNode; unit?: ReactNode; delta?: StatDelta; sparkline?: ReactNode; variant?: "card" | "soft" | "bare"; className?: string }`
- **Stepper** — Stepper.tsx — `StepperProps { steps: Step[]; current: number; orientation?: StepperOrientation; onStepClick?: (index: number) => void; className?: string }`
- **Switch** — Switch.tsx — `SwitchProps { checked: boolean; onChange?: (checked: boolean) => void; size?: SwitchSize; disabled?: boolean; label?: ReactNode; "aria-label"?: string; className?: string }`
- **Tab** — Tab.tsx — `TabProps { label: string; kind?: TabKind; active?: boolean; dirty?: boolean; onClose?: MouseEventHandler<HTMLButtonElement>; onClick?: MouseEventHandler<HTMLButtonElement>; className?: string; children?: ReactNode }`
- **TabBar** — TabBar.tsx — `TabBarProps { children: ReactNode; trailing?: ReactNode; className?: string }`
- **Table** — Table.tsx — `(no Props type)`
- **Tabs** — Tabs.tsx — `TabsProps { options: TabsOption[]; value: string; onChange: (value: string) => void; size?: TabsSize; className?: string }`
- **Tag** — Tag.tsx — `TagProps { children: ReactNode; variant?: TagVariant; size?: TagSize; onClose?: () => void; onClick?: () => void; className?: string }`
- **TextLink** — TextLink.tsx — `TextLinkProps { children: ReactNode; href?: string; onClick?: () => void; tone?: TextLinkTone; size?: TextLinkSize; trailingArrow?: boolean; external?: boolean; className?: string }`
- **Textarea** — Textarea.tsx — `(no Props type)`
- **Tile** — Tile.tsx — `TileProps { hero?: ReactNode; label?: ReactNode; value?: ReactNode; body?: ReactNode; trailing?: ReactNode; variant?: TileVariant; size?: TileSize; className?: string; onClick?: () => void }`
- **Toast** — Toast.tsx — `ToastProps { tone?: ToastTone; title: ReactNode; body?: ReactNode; action?: ReactNode; onClose?: () => void; className?: string }`
- **Tooltip** — Tooltip.tsx — `TooltipProps { children: ReactNode; content: ReactNode; shortcut?: ReactNode[]; variant?: TooltipVariant; placement?: TooltipPlacement; className?: string }`
- **IconX** — icons.tsx — `(no Props type)`
- **IconChevronDown** — icons.tsx — `(no Props type)`
- **IconChevronUp** — icons.tsx — `(no Props type)`
- **IconExternal** — icons.tsx — `(no Props type)`
- **IconArrowRight** — icons.tsx — `(no Props type)`
- **IconPin** — icons.tsx — `(no Props type)`
- **IconTrash** — icons.tsx — `(no Props type)`
- **IconGrip** — icons.tsx — `(no Props type)`
- **IconPanel** — icons.tsx — `(no Props type)`
- **IconExpand** — icons.tsx — `(no Props type)`

### @/components/patterns

- **ActionBar** — ActionBar.tsx — `ActionBarProps { primary?: ReactNode; secondary?: ReactNode; leading?: ReactNode; variant?: ActionBarVariant; className?: string }`
- **FilterBar** — FilterBar.tsx — `FilterBarProps { tabs?: FilterTab[]; activeTab?: string; onTabChange?: (value: string) => void; searchValue?: string; searchPlaceholder?: string; onSearchChange?: (value: string) => void; trailing?: ReactNode; className?: string }`
- **KeyValue** — KeyValue.tsx — `KeyValueProps { label: ReactNode; value: ReactNode; caption?: ReactNode; orientation?: "inline" | "stacked"; className?: string }`
- **DefinitionList** — KeyValue.tsx — `DefinitionListProps { items: DefinitionListItem[]; layout?: "horizontal" | "grid" | "stacked"; className?: string }`
- **PageHeader** — PageHeader.tsx — `PageHeaderProps { kicker?: ReactNode; subtitle?: ReactNode; subtitleVariant?: PageHeaderSubtitleVariant; title: ReactNode; titleSize?: HeadingSize; meta?: ReactNode; action?: ReactNode; className?: string }`
- **Prose** — Prose.tsx — `ProseProps { children: ReactNode; variant?: ProseVariant; gap?: StackGap; className?: string }`

### @/components/charts

- **BarChart** — BarChart.tsx — `BarChartProps { series: BarSeries[]; categories: string[]; orientation?: BarOrientation; yMax?: number; showLegend?: boolean; width?: number; height?: number; className?: string }`
- **DonutChart** — DonutChart.tsx — `DonutChartProps { segments: DonutSegment[]; centerLabel?: ReactNode; centerCaption?: ReactNode; showLegend?: boolean; size?: number; strokeWidth?: number; className?: string }`
- **LineChart** — LineChart.tsx — `LineChartProps { series: LineSeries[]; xLabels?: string[]; yMin?: number; yMax?: number; gridLines?: number; showLegend?: boolean; showDots?: boolean; width?: number; height?: number; className?: string }`
- **Sparkline** — Sparkline.tsx — `SparklineProps { values: number[]; width?: number; height?: number; tone?: SparklineTone; area?: boolean; dashed?: boolean; className?: string }`

### @/components/agent

- **AcceptRejectBar** — AcceptRejectBar.tsx — `AcceptRejectBarProps { variant?: AcceptRejectBarVariant; iteration?: { current: number; total: number }; label?: ReactNode; onAccept?: () => void; onReject?: () => void; onRegenerate?: () => void; acceptLabel?: ReactNode; className?: string }`
- **AgentAvatar** — AgentAvatar.tsx — `AgentAvatarProps { name?: string; state?: AgentAvatarState; size?: "sm" | "md"; trailingNote?: string; className?: string }`
- **AgentErrorCard** — AgentErrorCard.tsx — `AgentErrorCardProps { tone?: AgentErrorTone; title: ReactNode; body?: ReactNode; actions?: ReactNode; icon?: GlyphName; className?: string }`
- **CarouselCard** — CarouselCard.tsx — `CarouselCardProps { variants: CarouselVariant[]; state?: "preparing" | "ready" | "accepted"; acceptedIndex?: number; preparingLabel?: ReactNode; onAccept?: (index: number) => void; onReject?: () => void; onEdit?: () => void; className?: string }`
- **CitationLink** — CitationLink.tsx — `CitationLinkProps { number: number; preview?: ReactNode; href?: string; onClick?: () => void; className?: string }`
- **ConfidenceBadge** — ConfidenceBadge.tsx — `ConfidenceBadgeProps { value: number; variant?: ConfidenceBadgeVariant; tone?: ConfidenceBadgeTone; showLabel?: boolean; className?: string }`
- **DiffViewSideBySide** — DiffView.tsx — `DiffViewSideBySideProps { before: ReactNode; after: ReactNode; label?: ReactNode; className?: string }`
- **DiffRemoved** — DiffView.tsx — `(no Props type)`
- **DiffAdded** — DiffView.tsx — `(no Props type)`
- **HallucinationFlag** — HallucinationFlag.tsx — `HallucinationFlagProps { tone?: FlagTone; variant?: FlagVariant; label?: ReactNode; title?: ReactNode; body?: ReactNode; actions?: ReactNode; className?: string }`
- **MessageBubble** — MessageBubble.tsx — `MessageBubbleProps { role?: MessageRole; state?: MessageState; author?: string; timestamp?: string; children?: ReactNode; className?: string }`
- **MultiStepFlow** — MultiStepFlow.tsx — `MultiStepFlowProps { total: number; current: number; stepLabel?: ReactNode; question: ReactNode; options: MultiStepOption[]; value?: string; onChange?: (value: string) => void; onContinue?: () => void; onBack?: () => void; onSkip?: () => void; continueLabel?: ReactNode; className?: string }`
- **PromptBar** — PromptBar.tsx — `PromptBarProps { value?: string; onChange?: (value: string) => void; onSubmit?: (value: string) => void; onAttach?: () => void; attachments?: PromptBarAttachment[]; onRemoveAttachment?: (id: string) => void; placeholder?: string; showShortcut?: boolean; showSlashHint?: boolean; className?: string }`
- **ReasonedChip** — ReasonedChip.tsx — `ReasonedChipProps { label: ReactNode; steps?: ReactNode[]; sources?: ReactNode; defaultOpen?: boolean; className?: string }`
- **ReasoningTrace** — ReasoningTrace.tsx — `ReasoningTraceProps { steps: ReasoningStep[]; defaultOpen?: boolean; summary?: ReactNode; className?: string }`
- **SourceChip** — SourceChip.tsx — `SourceChipProps { kind?: SourceKind; file: ReactNode; pointer?: ReactNode; preview?: ReactNode; variant?: SourceChipVariant; onClick?: MouseEventHandler<HTMLDivElement>; className?: string }`
- **StreamingCursor** — StreamingCursor.tsx — `StreamingCursorProps { variant?: StreamingCursorVariant; className?: string }`
- **SuggestionPill** — SuggestionPill.tsx — `SuggestionPillProps { children: ReactNode; tone?: SuggestionPillTone; size?: SuggestionPillSize; onClick?: MouseEventHandler<HTMLButtonElement>; className?: string }`
- **ThinkingIndicator** — ThinkingIndicator.tsx — `ThinkingIndicatorProps { tone?: ThinkingIndicatorTone; size?: ThinkingIndicatorSize; label?: ReactNode; className?: string }`
- **ToolCallCard** — ToolCallCard.tsx — `ToolCallCardProps { name: string; args?: ReactNode; state?: ToolCallState; elapsed?: string; resultSummary?: ReactNode; results?: ReactNode[]; reason?: ReactNode; className?: string }`

## Color tokens (`globals.css`)

```
--color-ink: #1A1B1F
--color-muted: #525252
--color-faint: #858585
--color-faintest: #A6A6A6
--color-hairline: #E9E9E9
--color-hairline-strong: #D5DDE3
--color-soft: #F6F6F6
--color-stripe: #FAFBFC
--color-paper: #FFFFFF
--color-coral: #FF4E49
--color-coral-soft: #FFF5F4
--color-green: #43DD9C
--color-green-soft: #E4F6EC
--color-gold: #F6A600
--color-gold-soft: #FFF6E0
--color-pink: #FFD9E5
--color-pink-ink: #C13075
--color-info: #4FA8E4
--color-accent-indigo: #3935FF
--color-warning: #F6A600
--color-warning-soft: #FFF6E0
--color-text: var(--color-ink)
--color-text-muted: var(--color-muted)
--color-text-faint: var(--color-faint)
--color-text-faintest: var(--color-faintest)
--color-surface: var(--color-paper)
--color-surface-paper: var(--color-paper)
--color-surface-soft: var(--color-soft)
--color-surface-stripe: var(--color-stripe)
--color-border: var(--color-hairline)
--color-border-strong: var(--color-hairline-strong)
--color-accent: var(--color-coral)
--color-accent-soft: var(--color-coral-soft)
--color-success: var(--color-green)
--color-success-soft: var(--color-green-soft)
--color-danger: var(--color-coral)
--color-danger-soft: var(--color-coral-soft)
--color-walkthrough-line: oklch(0.665 0.220 24 / 0.42)
--color-walkthrough-dot: oklch(0.665 0.220 24 / 0.70)
```

## Animation utilities

Keyframes: `ads-fade-in`, `workspace-drawer-in`

Utility classes (apply to elements; do NOT invent new ones):
```
.anim-fade-in
.anim-fade-in-1
.anim-fade-in-2
.anim-fade-in-3
.anim-fade-in-4
.anim-fade-in-5
```

## Where to read the full rules

- Composition contract (audit-enforced): [`docs/COMPOSITION.md`](./COMPOSITION.md)
- Layout shells + Desktop resilience: [`docs/LAYOUT.md`](./LAYOUT.md)
- Navigation (`<Button>` vs `<LinkButton>` vs `<Link>`): [`docs/NAVIGATION.md`](./NAVIGATION.md)
- Spacing scale (`<Stack>`/`<Cluster>` gap buckets): [`docs/SPACING.md`](./SPACING.md)

